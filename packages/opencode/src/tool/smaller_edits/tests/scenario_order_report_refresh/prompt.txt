Refresh the order reporting module.

Work only in these files:
- `src/orders/report.ts`
- `src/orders/console.ts`
- `docs/orders-report.md`

Requirements:
1. Introduce a shared `statusLabel(status: OrderStatus)` helper in each code file and use it everywhere instead of repeated literal branches. Labels must be: `draft -> Draft`, `paid -> Paid`, `overdue -> Overdue`, `hold -> On hold`, `refunded -> Refunded`, `cancelled -> Cancelled`.
2. In `src/orders/report.ts`, add `agingLabel(daysOverdue: number)` returning `due now` for values less than or equal to 0, `1 day overdue` for 1, and `${n} days overdue` otherwise.
3. In `src/orders/report.ts`, implement dedicated helpers `renderDetailedStatus(order: Order)` and `renderSummaryStatus(order: Order)` that both start from `statusLabel(order.status)` and append ` (${agingLabel(order.daysOverdue)})` only for overdue orders. Use those helpers from `renderOrderRow` and `renderExpandedSummary`.
4. In `src/orders/console.ts`, add an `overdueSuffix(order: Order)` helper that returns `` for non-overdue orders or `daysOverdue <= 0`, and otherwise returns ` overdue=<days>d`. Keep the compact line field order the same; append the suffix to the money field so the example shape is `Overdue | $482.50 overdue=3d`, not `Overdue overdue=3d | $482.50`.
5. Update the docs examples and the supported statuses table to match the new labels and overdue wording. The console example should be exactly `ord_1002 | Acorn Bakery | Overdue | $482.50 overdue=3d`, and the closing explanatory sentence should mention both the detailed-view aging text and the console-view suffix.
6. Do not reorder the sample data.
